{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Goodreads Shelf Position Editor (Auto-Reload)",
      "type": "firefox",
      "request": "launch",
      "reAttach": true,
      "addonPath": "${workspaceFolder}/src",
      "reloadOnChange": {
        "watch": [
          "${workspaceFolder}/src/**/*.js",
          "${workspaceFolder}/src/**/*.json",
          "${workspaceFolder}/src/**/*.html",
          "${workspaceFolder}/src/**/*.css"
        ],
        "ignore": [
          "${workspaceFolder}/src/**/*.git/**"
        ]
      }
    },
    {
      "name": "Debug Goodreads Shelf Position Editor",
      "type": "firefox",
      "request": "launch",
      "reAttach": true,
      "addonPath": "${workspaceFolder}/src"
    }
  ]
}
